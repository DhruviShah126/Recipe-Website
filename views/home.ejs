<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div >
        <h1 class="display-5 fw-bold mb-3">Vegetarian Eats</h1>
        <p class="lead">
            Explore the selection of vegetarain recipes including: easy desserts, quick dinner ideas, and recipes from a multitude of cuisines to choose from!
        </p>

        <div class="d-grip gap-2 d-md-flex jusitfy-content-md-start">
            <a href="/latest-recipes" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">View Latest Recipes</a>
            <a href="/random-recipe" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Try a Random Recipe</a>
        </div>
    </div>
</div>

<!-- Categories Section -->

<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
    <% if(categories != '') { %>
        <% categories.forEach(function(category, index) { %>
            <a href="/categories/<%- category.name %> " class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="img/<%- category.image %>" alt="<%- category.name %>" Loading="lazy" >
                </div>
                <div class="pt-1"><%- category.name %></div>
            </a>
        <% }) %>
    <% } %>
    <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
            <img src="img/view_all.jpg" alt="View All Food" Loading="lazy">
        </div>
        <div class="pt-1">View All</div>
    </a>
</div>

<!-- Categories Section End -->

<!-- Latest Recipies -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/latest-recipes" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food != '') { %>
            <% food.latest.forEach(function(recipe, index) { %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>
                </a>
            <% }) %>
        <% } %>
    </div>
</section>
<!-- Latest Recipies End-->

<!-- Mexican Recipies -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Mexican Recipes</h2>
        <a href="/categories/mexican" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.mexican !== 'undefined' && food.mexican.length > 0) { %>
            <% food.mexican.forEach(function(recipe, index) { %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No recipes found</p>
        <% } %>
    </div>
</section>
<!-- Mexican Recipies End-->

<!-- Indian Recipies -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Indian Recipes</h2>
        <a href="/categories/indian" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.indian !== 'undefined' && food.indian.length > 0) { %>
            <% food.indian.forEach(function(recipe, index) { %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No recipes found</p>
        <% } %>
    </div>
</section>
<!-- Indian Recipies End-->

<!-- Dessert Recipies -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Dessert Recipes</h2>
        <a href="/categories/dessert" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food.dessert !== 'undefined' && food.dessert.length > 0) { %>
            <% food.dessert.forEach(function(recipe, index) { %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No recipes found</p>
        <% } %>
    </div>
</section>
<!-- Dessert Recipies End-->

<!-- Share a Recipe -->
<section class="px-4 py-5 my-5 text-center">
    <h1 class="display-5 fw-bold">Share your delicious recipe!</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Submit your recipe and share your favorite meals with others </p>
        <div class="d-grip gap-2 d-sm-flex justify-content-sm-center"></div>
        <a href="/share-recipe" class="btn btn-primary btn-dark btn-lg">Share Recipe</a>
    </div>
</section>

<!-- Share a Recipe End-->